language: go

go:
  - 1.5
  - tip

services:
  - mongodb

script:
  - make pre-commit
  - make build
  - make test

notifications:
  email:
    on_success: change
    on_failure: always

matrix:
  allow_failures:
    - go: tip
