os: linux

dist: jammy

language: go

go:
  - 1.23.4

env:
  global:
    - MONGODB=6.0.19
    - MONGOSH=2.3.8
  jobs:
    - PLUGINS_VISIBILITY=public
    - PLUGINS_VISIBILITY=private

cache:
  directories:
    - $HOME/.cache/go-build

before_install:
  - . .travis/cache-setup.sh

before_cache:
  - . .travis/cache-teardown.sh

script:
  - echo "Hello"
